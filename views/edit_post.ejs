<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel = "stylesheet" href = "styles/all.min.css">
    <link rel = "stylesheet" href =  "styles/normalize.css">
    <link rel = "stylesheet" href = "styles/new-post.css">
    <link rel = "stylesheet" href = "styles/internal_header.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@200;300;400;500;600;700;800&family=Inter:wght@300;400;500;600;700&family=Rubik:ital,wght@0,300;0,500;0,600;0,700;0,800;0,900;1,400&display=swap" rel = "stylesheet">
    <title>New Post</title>
</head>
<body>
    <div id = "overlay"></div>
    <%- include("internal_header.ejs", {showLink : true, showSearch : false})%>
    <div class = "main">
        <% if (locals.state) { %>
            <div id = "response" data-state = "<%= locals.state %>" data-type = "<%= locals.type %>">
                <% if (type === "error")  {%>
                    <i class="fa-solid fa-circle-check" style="color: #f00;"></i>
                    <span id = "error"><%= state %></span>
                <% } else { %>
                    <i class="fa-solid fa-circle-check" style="color: #63E6BE;"></i>
                    <span id = "success"><%= state %></span>
                <% } %>
            </div>
        <% } %>
        <% if (locals.post) {%>
       <div class="post">
        <h3>New Post</h3>
        <form action = "./edit-post" method = "post">
            <div>
                <label for = "title"><%= post.title %></label>
                <input type="text" name = "title">
            </div>
            <div>
                <label for = "post">Post</label>
                <textarea name = "content" cols="30" rows="10"><%= post.content %></textarea>
            </div>
            <div>
                <input type = "submit" value = "Edit" name = "post">
                <button type = "submit">Back</button>
            </div>
        </form>
       </div>
       <% } %>
    </div>
    <div class = "footer">
        <h3>MyBlog</h3>
        <p>Copyright &copy; for 0xnaif</p>
    </div>
    <script src = "./new-post.js"></script>
</body>
</html>